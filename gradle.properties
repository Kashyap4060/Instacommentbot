android.useAndroidX=true
android.enableJetifier=true
# Give the Gradle daemon a real heap — the default (~512m) OOMs during APK
# packaging (zipflinger/ApkFlinger) on this machine, especially under memory pressure.
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8
